{
  "name": "portalflow-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd apps/api && source .venv/bin/activate && uvicorn main:app --reload",
    "install:all": "pnpm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
