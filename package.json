{
  "name": "portalflow-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter upload-portal dev",
    "dev:api": "cd connector-hub && source venv/bin/activate && uvicorn main:app --reload --port 8000",
    "web": "pnpm --filter upload-portal",
    "api": "cd connector-hub && source venv/bin/activate"
  },
  "dependencies": {},
  "devDependencies": {}
}
