openapi: 3.1.0
info:
  title: AI Connector Hub API
  version: 0.1.0
paths:
  /execute:
    post:
      operationId: executeAction
      summary: Execute connector action
  /search:
    post:
      operationId: searchWorkspace
      summary: Search connected systems
